import { RegisterForm } from "@/components/auth/register-form";

export const metadata = {
  title: "Register",
  description: "Create a new account to join us now.",
};

const RegisterPage = () => {
  return <RegisterForm />;
};

export default RegisterPage;
